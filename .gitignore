# Rust build artifacts
/target

# Distribution build artifacts (generated by scripts/build-*.sh and CI).
# dist/brew/ (Homebrew cask formula) is hand-maintained and IS versioned.
/dist/macos/
/dist/linux/
/dist/windows/

# macOS
.DS_Store

# Editor / misc
*.rs.bk
*.swp

# Local-only artifacts (release zips built outside CI, Playwright MCP output)
/Glanvu-*.zip
/.playwright-mcp/

# Graphify knowledge graph (generated, regenerable via `graphify .`)
graphify-out/

# Note: Cargo.lock IS committed (this workspace ships a binary).
